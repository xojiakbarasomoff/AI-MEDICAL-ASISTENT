# In-container config: used by the api/worker services via `env_file` in
# docker-compose.yml. Containers reach Postgres/Redis by service name on the
# internal Docker network (always port 5432/6379 there) — the host-side port
# remap to 5433 for Postgres does not apply inside the compose network.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/dental_ig_assistant
REDIS_URL=redis://redis:6379/0
OPENAI_API_KEY=sk-your-key-here
WEBHOOK_VERIFY_TOKEN=change-me-verify-token
META_APP_SECRET=change-me-app-secret
